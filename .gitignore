# build artifacts
*.o
*.exe
ldpc_sim
tests/selftest

# simulation output
results/
*.csv

# generated plots: ignore stray outputs (e.g. MATLAB saveas in the repo root),
# but keep the curated result figures under figures/
*.png
!figures/*.png

# python virtual environments
.venv/
__pycache__/

# OS / editor cruft
.DS_Store
*.swp

# Report PDF is excluded until it is fully de-identified. Student ID is gone,
# but the real name still leaks via the per-page footer ("Jyun-Hong Pan") and
# the PDF /Author metadata. Re-export from the slide source with the footer
# changed to TimmyPan, then delete this line and commit it.
docs/LDPC_Project_Report.pdf
