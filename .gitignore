# build artifacts
*.o
*.exe
ldpc_sim

# simulation output
results/
*.csv

# generated plots: ignore stray outputs (e.g. MATLAB saveas in the repo root),
# but keep the curated result figures under figures/
*.png
!figures/*.png

# python virtual environments
.venv/
__pycache__/

# OS / editor cruft
.DS_Store
*.swp

# Report PDF is excluded until its INTERNAL text is redacted (real name +
# student ID still appear inside the file). Regenerate an anonymized version
# (author = TimmyPan, no student ID), then delete this line and commit it.
docs/LDPC_Project_Report.pdf
